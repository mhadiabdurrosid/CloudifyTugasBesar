# ============================================
# 🌍 ENVIRONMENT MODE
# ============================================
APP_ENV=local   # local | production

# ============================================
# 💻 LOCAL DATABASE (XAMPP / Laragon)
# ============================================
LOCAL_DB_HOST=localhost
LOCAL_DB_USER=root
LOCAL_DB_PASS=
LOCAL_DB_NAME=cloudify
LOCAL_DB_PORT=3306

# ============================================
# ☁️ RAILWAY DATABASE (Auto Injected by Railway)
# Railway akan mengisi ENV ini secara otomatis.
# Jangan ubah kecuali ingin override manual.
# ============================================
DB_HOST=${MYSQLHOST}
DB_USER=${MYSQLUSER}
DB_PASS=${MYSQLPASSWORD}
DB_NAME=${MYSQLDATABASE}
DB_PORT=${MYSQLPORT}
